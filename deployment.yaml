apiVersion: apps/v1
kind: Deployment
metadata:
  name: safebank-web
  labels:
    app: safebank-web
    version: v1.0
spec:
  replicas: 3
  selector:
    matchLabels:
      app: safebank-web
  template:
    metadata:
      labels:
        app: safebank-web
    spec:
      containers:
      - name: safebank-web
        image: nginx:1.25-alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        volumeMounts:
        - name: safebank-content
          mountPath: /usr/share/nginx/html
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: safebank-content
        configMap:
          name: safebank-html
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: safebank-html
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="pt-BR">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>SafeBank Digital - Infraestrutura K8s</title>
        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }
            body {
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
            }
            .container {
                text-align: center;
                padding: 2rem;
                background: rgba(255, 255, 255, 0.1);
                border-radius: 15px;
                backdrop-filter: blur(10px);
                box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37);
                border: 1px solid rgba(255, 255, 255, 0.18);
                max-width: 600px;
            }
            h1 {
                font-size: 3rem;
                margin-bottom: 1rem;
                text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            }
            .subtitle {
                font-size: 1.5rem;
                margin-bottom: 2rem;
                opacity: 0.9;
            }
            .info-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                gap: 1rem;
                margin: 2rem 0;
            }
            .info-card {
                background: rgba(255, 255, 255, 0.15);
                padding: 1rem;
                border-radius: 10px;
                border: 1px solid rgba(255, 255, 255, 0.2);
            }
            .status {
                font-size: 1.2rem;
                font-weight: bold;
                color: #4ade80;
                margin-top: 2rem;
            }
            .footer {
                margin-top: 2rem;
                font-size: 0.9rem;
                opacity: 0.7;
            }
            @keyframes pulse {
                0% { opacity: 1; }
                50% { opacity: 0.7; }
                100% { opacity: 1; }
            }
            .pulse {
                animation: pulse 2s infinite;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>🏛️ SafeBank Digital</h1>
            <p class="subtitle">Kubernetes Infrastructure Validation</p>
            
            <div class="info-grid">
                <div class="info-card">
                    <h3>🚀 Platform</h3>
                    <p>Kubernetes on AWS EC2</p>
                </div>
                <div class="info-card">
                    <h3>🔧 Runtime</h3>
                    <p>containerd + Flannel CNI</p>
                </div>
                <div class="info-card">
                    <h3>⚡ Service</h3>
                    <p>NodePort (Porta 30080)</p>
                </div>
                <div class="info-card">
                    <h3>📊 Replicas</h3>
                    <p>3 Pod Instances</p>
                </div>
            </div>
            
            <div class="status pulse">
                ✅ Infrastructure Ready for Development Team
            </div>
            
            <div class="footer">
                <p>Container ID: <span id="hostname">N/A</span></p>
                <p>Deployed via Kubernetes Deployment</p>
            </div>
        </div>
        
        <script>
            // Simula exibição do hostname do container
            fetch('/').then(() => {
                document.getElementById('hostname').textContent = 
                    Math.random().toString(36).substring(2, 15);
            }).catch(() => {
                document.getElementById('hostname').textContent = 'safebank-web-pod';
            });
        </script>
    </body>
    </html>